services:
  postgres:
    image: postgres:16
    container_name: java_db
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: employeemanager
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres